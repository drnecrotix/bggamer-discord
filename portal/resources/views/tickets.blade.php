<!doctype html><html lang="bg"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Tickets · BG-GAMER</title><link rel="stylesheet" href="{{ url('/portal.css') }}"></head><body><main class="editor"><a href="{{ url('/admin') }}">← Dashboard</a><p class="eyebrow">COMMUNITY / SUPPORT</p><h1>Support tickets</h1>@if(session('status'))<p class="notice success">{{ session('status') }}</p>@endif@forelse($tickets as $ticket)<article class="panel" style="margin-top:16px;min-height:0"><small>{{ $ticket->reference }} · {{ $ticket->created_at }} · {{ $ticket->status }}</small><h2>{{ $ticket->subject }}</h2><p class="muted">{{ $ticket->email }} @if($ticket->discord_user_id) · Discord: {{ $ticket->discord_user_id }}@endif</p><p style="white-space:pre-wrap">{{ $ticket->message }}</p><form method="post" action="{{ url('/admin/tickets/'.$ticket->id) }}">@csrf<label>Статус</label><select name="status"><option value="open" @selected($ticket->status==='open')>Open</option><option value="in_progress" @selected($ticket->status==='in_progress')>In progress</option><option value="resolved" @selected($ticket->status==='resolved')>Resolved</option></select><label>Отговор за потребителя</label><textarea name="reply" maxlength="5000">{{ $ticket->reply }}</textarea><button class="button">Запази</button></form></article>@empty<p class="muted">Няма tickets.</p>@endforelse<div class="pagination">{{ $tickets->links() }}</div></main></body></html>
