<!doctype html><html lang="bg"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Discord Server · BG-GAMER</title><link rel="stylesheet" href="{{ url('/portal.css') }}"><main class="editor"><a href="{{ url('/admin') }}">← Dashboard</a><p class="eyebrow">INTEGRATIONS / DISCORD</p><h1>Server settings</h1><p class="muted">Въведеният сървър захранва публичните статистики, admin dashboard и проверката на staff роли. Ботът трябва да е член на този сървър.</p>@if(session('status'))<p class="notice success">{{ session('status') }}</p>@endif<form method="post" action="{{ url('/admin/server') }}">@csrf<label for="guild_id">Discord Server ID</label><input id="guild_id" name="guild_id" inputmode="numeric" pattern="[0-9]{17,20}" value="{{ old('guild_id', $settings['guild_id']) }}" required>@error('guild_id')<p class="error">{{ $message }}</p>@enderror<label for="invite_code">Invite code</label><input id="invite_code" name="invite_code" value="{{ old('invite_code', $settings['invite_code']) }}" placeholder="PFkjeKBuxH"><p class="muted">Само кодът след discord.gg/. Не въвеждай bot token или secret тук.</p>@error('invite_code')<p class="error">{{ $message }}</p>@enderror<button class="button">Провери и запази</button> <a href="{{ url('/') }}" target="_blank" rel="noopener">Виж landing page ↗</a></form></main></html>
