<!doctype html><html lang="bg" data-bs-theme="dark"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Support · BG-GAMER</title><link rel="stylesheet" href="{{ url('/bootstrap.min.css') }}"><link rel="stylesheet" href="{{ url('/portal.css') }}"></head><body><main class="editor container"><a href="{{ url('/') }}">← BG-GAMER</a><p class="eyebrow mt-4">COMMUNITY / SUPPORT</p><h1>Поддръжка</h1><p class="muted">За бан използвай <a href="{{ url('/appeal/') }}">формуляра за обжалване</a>. За други въпроси изпрати ticket.</p>@if(session('ticket_reference'))<p class="notice success">Получихме заявката. Запази номер {{ session('ticket_reference') }} за контакт с екипа.</p>@endif@if($errors->any())<p class="error">{{ $errors->first() }}</p>@endif<section class="panel"><form method="post" action="{{ url('/support') }}">@csrf<div class="row g-3"><div class="col-md-6"><label for="email">Имейл за отговор</label><input id="email" name="email" type="email" required maxlength="255" value="{{ old('email') }}"></div><div class="col-md-6"><label for="discord_user_id">Discord User ID (по избор)</label><input id="discord_user_id" name="discord_user_id" inputmode="numeric" value="{{ old('discord_user_id') }}"></div></div><label for="subject">Тема</label><input id="subject" name="subject" required minlength="5" maxlength="160" value="{{ old('subject') }}"><label for="message">Съобщение</label><textarea id="message" name="message" required minlength="20" maxlength="5000">{{ old('message') }}</textarea><div style="position:absolute;left:-9999px" aria-hidden="true"><label>Website<input name="website" tabindex="-1" autocomplete="off"></label></div><button class="button">Изпрати ticket</button></form></section><section class="panel mt-4"><h2>Провери заявка</h2><p class="muted">Въведи номера от потвърждението и имейла, с който е подадена.</p><form method="post" action="{{ url('/support/check') }}">@csrf<label>Номер</label><input name="reference" required placeholder="BG-XXXXXXXXXXXX"><label>Имейл</label><input name="email" type="email" required><button class="button">Провери</button></form>@if(session('lookup'))@if(session('lookup.error'))<p class="error">{{ session('lookup.error') }}</p>@else<p class="notice">{{ session('lookup.reference') }} · {{ session('lookup.status') }}</p>@if(session('lookup.reply'))<p style="white-space:pre-wrap">{{ session('lookup.reply') }}</p>@endif@endif@endif</section></main></body></html>
