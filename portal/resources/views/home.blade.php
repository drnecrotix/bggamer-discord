<!doctype html><html lang="bg"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="{{ $page->subtitle }}"><title>{{ $page->title }} · BG-GAMER</title><link rel="stylesheet" href="{{ url('/portal.css') }}"><div class="public-nav"><strong class="brand">BG<span>GAMER</span></strong><nav><a href="{{ url('/') }}">Начало</a><a href="{{ url('/bans') }}">Ban list</a><a href="{{ url('/auth/discord') }}">Staff panel ↗</a><a class="owner-link" href="{{ url('/owner/login') }}">Owner</a></nav></div><main class="landing"><div class="landing-intro"><div class="landing-copy"><p class="eyebrow"><span class="live-dot"></span> BG-GAMER / DISCORD COMMUNITY</p><h1>{{ $page->title }}<span class="accent">.</span></h1><h2>{{ $page->subtitle }}</h2><p class="landing-description">{{ $page->body }}</p><div class="landing-actions">@if($inviteUrl)<a class="button" href="{{ $inviteUrl }}" target="_blank" rel="noopener noreferrer">Присъедини се в Discord ↗</a>@endif<a class="secondary-link" href="{{ url('/bans') }}">Правила и ban list →</a></div></div><div class="server-console"><div class="console-top"><span>SERVER / LIVE FEED</span><span class="console-indicator">{{ $guild || $widget ? '● CONNECTED' : '○ OFFLINE' }}</span></div><div class="console-body"><p class="eyebrow">COMMUNITY STATUS</p><h3>{{ $guild['name'] ?? $widget['name'] ?? 'BG-GAMER' }}</h3><p class="console-caption">{{ $guild || $widget ? 'Данни от Discord · обновяват се на 60 секунди' : 'Няма потвърдени данни от Discord в момента' }}</p><div class="live-feature"><div class="live-number">{{ isset($guild['approximate_presence_count']) ? number_format($guild['approximate_presence_count'], 0, '.', ' ') : (isset($widget['presence_count']) ? number_format($widget['presence_count'], 0, '.', ' ') : '—') }} <span>LIVE</span></div><p>@if($voiceCount !== null){{ $voiceCount }} {{ $voiceCount === 1 ? 'потребител се вижда' : 'потребители се виждат' }} в гласови канали в момента.@elseГласовите данни не са достъпни. Включи Server Widget в Discord настройките.@endif</p>@if(count($members))<div class="live-avatars" aria-label="Видими онлайн членове">@foreach($members as $member)<img src="{{ $member['avatar'] }}" alt="{{ $member['name'] }}" title="{{ $member['name'] }}" loading="lazy" referrerpolicy="no-referrer">@endforeach</div>@endif</div><div class="stat-lines"><div><span>MEMBERS</span><strong>{{ isset($guild['approximate_member_count']) ? number_format($guild['approximate_member_count'], 0, '.', ' ') : '—' }}</strong></div><div><span>BOOSTS</span><strong>{{ $guild['premium_subscription_count'] ?? '—' }}</strong></div></div></div><div class="console-bottom"><span>DISCORD SERVER</span><span>{{ $guild || $widget ? 'LIVE METRICS' : 'AWAITING CONNECTION' }}</span></div></div></div><section class="landing-lower"><div><p class="eyebrow">THE LOBBY IS OPEN</p><h2>Намери отбор.<br>Остани за общността.</h2></div><p>Игри, гласови канали, събития и хора с общи интереси. Данните вдясно идват от сървъра, избран в административния панел.</p></section></main></html>
