<!doctype html><html lang="bg" data-bs-theme="dark"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Обжалване · BG-GAMER</title><link rel="stylesheet" href="{{ url('/bootstrap.min.css') }}"><link rel="stylesheet" href="{{ url('/portal.css') }}"></head><body><main class="editor container"><a href="{{ url('/bans') }}">← Ban list</a><p class="eyebrow mt-4">COMMUNITY / APPEAL</p><h1>Обжалване на бан</h1><p class="muted">Опиши случая подробно. Ако знаеш публичния номер на бана, въведи го с Discord User ID от същия запис.</p>@if(session('appeal_reference'))<p class="notice success">Обжалването е получено. Запази номер {{ session('appeal_reference') }}.</p>@endif@if($errors->any())<p class="error">{{ $errors->first() }}</p>@endif<section class="panel"><form method="post" action="{{ url('/appeals') }}">@csrf<label>Ban reference (по избор)</label><input name="ban_reference" maxlength="24" value="{{ old('ban_reference', $reference) }}"><div class="row g-3"><div class="col-md-6"><label>Discord User ID</label><input name="discord_user_id" inputmode="numeric" required value="{{ old('discord_user_id') }}"></div><div class="col-md-6"><label>Discord име</label><input name="discord_username" required maxlength="80" value="{{ old('discord_username') }}"></div></div><label>Контакт за отговор</label><input name="contact" required maxlength="190" value="{{ old('contact') }}"><label>Причина за обжалване</label><input name="appeal_reason" required maxlength="190" value="{{ old('appeal_reason') }}"><label>Подробно обяснение</label><textarea name="additional_information" required minlength="20" maxlength="5000">{{ old('additional_information') }}</textarea><div style="position:absolute;left:-9999px" aria-hidden="true"><label>Website<input name="website" tabindex="-1" autocomplete="off"></label></div><button class="button">Изпрати обжалване</button></form></section><section class="panel mt-4"><h2>Провери обжалване</h2><form method="post" action="{{ url('/appeals/check') }}">@csrf<label>Номер на обжалването</label><input name="reference" required placeholder="AP-XXXXXXXXXXXXXX"><label>Discord User ID</label><input name="discord_user_id" required inputmode="numeric"><button class="button">Провери</button></form>@if(session('lookup'))@if(session('lookup.error'))<p class="error">{{ session('lookup.error') }}</p>@else<p class="notice">{{ session('lookup.reference') }} · {{ session('lookup.status') }}</p>@if(session('lookup.reply'))<p style="white-space:pre-wrap">{{ session('lookup.reply') }}</p>@endif@endif@endif</section></main></body></html>
