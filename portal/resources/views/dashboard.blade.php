<!doctype html><html lang="bg"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Dashboard · BG-GAMER</title><link rel="stylesheet" href="/portal.css"><div class="shell"><aside><strong class="brand">BG<span>GAMER</span></strong><small>DISCORD PORTAL</small><nav><a class="selected" href="/admin">◉ &nbsp; Overview</a>@if(session('staff.level') !== 'support')<a href="/admin/homepage">✎ &nbsp; Homepage editor</a>@endif<a href="/bans">↗ &nbsp; Public ban list</a><a href="https://discord.com/channels/{{ config('discord.guild_id') }}">↗ &nbsp; Discord server</a><a href="https://bg-gamer.com/discord/">↗ &nbsp; Community site</a></nav><div class="aside-bottom">{{ session('staff.name') }} <small>{{ strtoupper(session('staff.level')) }}</small><form action="/logout" method="post">@csrf<button class="text-button">Изход</button></form></div></aside><main><header><div><p class="eyebrow">SERVER CONTROL / LIVE DATA</p><h1>Dashboard<span class="accent">.</span></h1><p>Състояние на общността и инструменти за екипа.</p></div><span class="status">{{ $error ? '● CONNECTION ISSUE' : '● DISCORD CONNECTED' }}</span></header>@if($error)<div class="notice">{{ $error }}</div>@endif@if(session('status'))<div class="notice success">{{ session('status') }}</div>@endif<section class="metrics"><article><small>MEMBERS</small><strong>{{ $guild['approximate_member_count'] ?? '—' }}</strong><span>Общ брой в сървъра</span></article><article><small>ONLINE NOW</small><strong>{{ $guild['approximate_presence_count'] ?? '—' }}</strong><span>Активни членове</span></article><article><small>BOOSTS</small><strong>{{ $guild['premium_subscription_count'] ?? '—' }}</strong><span>Level {{ $guild['premium_tier'] ?? '—' }}</span></article><article><small>EVENTS</small><strong>{{ $error ? '—' : count($events) }}</strong><span>Планирани в Discord</span></article></section><div class="columns"><section class="panel"><div class="panel-head"><div><p class="eyebrow">UPCOMING</p><h2>Server events</h2></div><span>DISCORD API</span></div>@forelse($events as $event)<div class="row"><div><b>{{ $event['name'] ?? 'Event' }}</b><small>{{ isset($event['scheduled_start_time']) ? \Illuminate\Support\Carbon::parse($event['scheduled_start_time'])->format('d.m.Y H:i') : '—' }} UTC</small></div><span>↗</span></div>@empty<p class="muted">Няма планирани събития.</p>@endforelse</section><section class="panel"><div class="panel-head"><div><p class="eyebrow">BOT ACTIONS</p><h2>Announcement</h2></div><span>ADMIN ONLY</span></div>@if(session('staff.level') === 'admin')<form method="post" action="/announcements">@csrf<label for="content">Съобщение до настроения канал</label><textarea id="content" name="content" maxlength="1900" required placeholder="Напиши кратко съобщение за общността...">{{ old('content') }}</textarea>@error('content')<p class="error">{{ $message }}</p>@enderror<p class="muted">Споменаванията на @everyone и роли са изключени.</p><button class="button">Публикувай в Discord ↗</button></form>@else<p class="muted">Публикуването е достъпно за Admin роля.</p>@endif</section></div><section class="panel audit"><div class="panel-head"><div><p class="eyebrow">ACCOUNTABILITY</p><h2>Recent actions</h2></div></div>@forelse($activity as $entry)<div class="row"><b>{{ $entry->action }}</b><small>{{ $entry->actor_id }} · {{ $entry->created_at }}</small></div>@empty<p class="muted">Все още няма действия.</p>@endforelse</section></main></div></html>
