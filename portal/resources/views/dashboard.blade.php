<!doctype html><html lang="bg"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Dashboard · BG-GAMER</title><link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">@livewireStyles<link rel="stylesheet" href="{{ url('/portal.css') }}"><div class="shell"><aside><strong class="brand">BG<span>GAMER</span></strong><small>DISCORD PORTAL</small><nav><a class="selected" href="{{ url('/admin') }}">◉ &nbsp; Overview</a>@if(session('staff.level') !== 'support')<a href="{{ url('/admin/homepage') }}">✎ &nbsp; Homepage editor</a><a href="{{ url('/admin/knowledge') }}">? &nbsp; FAQ editor</a>@endif@if(session('staff.level') === 'admin')<a href="{{ url('/admin/server') }}">◈ &nbsp; Discord server</a>@endif<a href="{{ url('/bans') }}">↗ &nbsp; Public ban list</a><a href="{{ url('/commands') }}">⌘ &nbsp; Commands & FAQ</a><a href="{{ url('/admin/tickets') }}">✉ &nbsp; Support tickets</a>@if(session('owner_id'))<a href="{{ url('/owner/settings') }}">◎ &nbsp; Owner access</a><a href="{{ url('/owner/updates') }}">↻ &nbsp; CMS updates @if($updateAvailable)<b class="update-badge">NEW</b>@endif</a>@endif<a href="https://discord.com/channels/{{ $server['guild_id'] }}">↗ &nbsp; Discord server</a><a href="https://bg-gamer.com/discord/">↗ &nbsp; Community site</a></nav><div class="aside-bottom">{{ session('staff.name') }} <small>{{ strtoupper(session('staff.level')) }}</small><form action="{{ url('/logout') }}" method="post">@csrf<button class="text-button">Изход</button></form></div></aside><main><header><div><p class="eyebrow">SERVER CONTROL / LIVE DATA</p><h1>Dashboard<span class="accent">.</span></h1><p>Състояние на общността и инструменти за екипа.</p></div><span class="status">{{ $error ? '● CONNECTION ISSUE' : '● DISCORD CONNECTED' }}</span></header>@if($error)<div class="notice">{{ $error }}</div>@endif@if(session('status'))<div class="notice success">{{ session('status') }}</div>@endif<livewire:guild-status /><div class="columns"><section class="panel"><div class="panel-head"><div><p class="eyebrow">UPCOMING</p><h2>Server events</h2></div><span>DISCORD API</span></div>@forelse($events as $event)<div class="row"><div><b>{{ $event['name'] ?? 'Event' }}</b><small>{{ isset($event['scheduled_start_time']) ? \Illuminate\Support\Carbon::parse($event['scheduled_start_time'])->format('d.m.Y H:i') : '—' }} UTC</small></div><span>↗</span></div>@empty<p class="muted">Няма планирани събития.</p>@endforelse</section><section class="panel"><div class="panel-head"><div><p class="eyebrow">BOT ACTIONS</p><h2>Announcement</h2></div><span>ADMIN ONLY</span></div>@if(session('staff.level') === 'admin')<form method="post" action="{{ url('/announcements') }}">@csrf<label for="content">Съобщение до настроения канал</label><textarea id="content" name="content" maxlength="1900" required placeholder="Напиши кратко съобщение за общността...">{{ old('content') }}</textarea>@error('content')<p class="error">{{ $message }}</p>@enderror<p class="muted">Споменаванията на @everyone и роли са изключени.</p><button class="button">Публикувай в Discord ↗</button></form>@else<p class="muted">Публикуването е достъпно за Admin роля.</p>@endif</section></div><section class="panel audit"><div class="panel-head"><div><p class="eyebrow">ACCOUNTABILITY</p><h2>Recent actions</h2></div></div>@forelse($activity as $entry)<div class="row"><b>{{ $entry->action }}</b><small>{{ $entry->actor_id }} · {{ $entry->created_at }}</small></div>@empty<p class="muted">Все още няма действия.</p>@endforelse</section></main></div>@livewireScripts</html>
