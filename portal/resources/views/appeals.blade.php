<!doctype html><html lang="bg"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Ban appeals · BG-GAMER</title><link rel="stylesheet" href="{{ url('/portal.css') }}"></head><body><main class="editor"><a href="{{ url('/admin') }}">← Dashboard</a><p class="eyebrow">MODERATION / APPEALS</p><h1>Ban appeals</h1>@if(session('status'))<p class="notice success">{{ session('status') }}</p>@endif@forelse($appeals as $appeal)<article class="panel" style="margin-top:16px;min-height:0"><small>{{ $appeal->public_reference }} · {{ $appeal->submitted_at }} · {{ $appeal->status }}</small><h2>{{ $appeal->discord_username }}</h2><p class="muted">Discord ID: {{ $appeal->discord_user_id }} · Contact: {{ $appeal->contact }}</p><p><strong>{{ $appeal->appeal_reason }}</strong></p><p style="white-space:pre-wrap">{{ $appeal->additional_information }}</p><form method="post" action="{{ url('/admin/appeals/'.$appeal->id) }}">@csrf<label>Статус</label><select name="status">@foreach(['pending','under_review','information_requested','approved','rejected','closed'] as $status)<option value="{{ $status }}" @selected($appeal->status===$status)>{{ $status }}</option>@endforeach</select><label>Отговор за потребителя</label><textarea name="moderator_response" maxlength="5000">{{ $appeal->moderator_response }}</textarea><button class="button">Запази</button></form></article>@empty<p class="muted">Няма обжалвания.</p>@endforelse<div class="pagination">{{ $appeals->links() }}</div></main></body></html>
