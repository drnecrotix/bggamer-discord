<!doctype html><html lang="bg"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Homepage editor · BG-GAMER</title><link rel="stylesheet" href="/portal.css"><main class="editor"><a href="/admin">← Dashboard</a><p class="eyebrow">CONTENT / PUBLIC PAGE</p><h1>Homepage editor</h1><p class="muted">Промените се виждат на началната страница на този портал. Публикувай след преглед.</p>@if(session('status'))<p class="notice success">{{ session('status') }}</p>@endif<form action="/admin/homepage" method="post">@csrf@foreach(['title'=>'Заглавие','subtitle'=>'Подзаглавие','body'=>'Основен текст','cta_label'=>'Текст на бутона','cta_url'=>'Адрес на бутона'] as $field=>$label)<label for="{{ $field }}">{{ $label }}</label>@if($field==='body')<textarea id="body" name="body" maxlength="3000" required>{{ old($field,$page->$field) }}</textarea>@else<input id="{{ $field }}" name="{{ $field }}" value="{{ old($field,$page->$field) }}" required>@endif@error($field)<p class="error">{{ $message }}</p>@enderror@endforeach<button class="button">Запази промените</button> <a href="/" target="_blank" rel="noopener">Преглед ↗</a></form></main></html>
