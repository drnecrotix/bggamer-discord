<!doctype html><html lang="bg"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>CMS updates · BG-GAMER</title><link rel="stylesheet" href="/portal.css"><main class="editor"><a href="/admin">← Dashboard</a><p class="eyebrow">SYSTEM / GITHUB RELEASES</p><h1>CMS updates</h1><p class="muted">Текуща версия: {{ $current }}. Само Owner може да приложи проверен code-only release пакет. Архив с миграции или нови зависимости се инсталира ръчно след backup.</p>@if(session('status'))<p class="notice success">{{ session('status') }}</p>@endif@if($errors->any())<p class="error">{{ $errors->first() }}</p>@endif<section class="panel"><h2>{{ $release['version'] ?? 'Няма публикуван release' }}</h2>@if($release && ($release['ready'] ?? false))<p class="muted">GitHub SHA-256: {{ $release['digest'] }}</p><form method="post" action="/owner/updates">@csrf<input type="hidden" name="version" value="{{ $release['version'] }}"><button class="button">Провери и приложи обновяването</button></form>@else<p class="muted">Очаква се GitHub Release с asset <code>bggamer-portal.zip</code> и SHA-256 digest.</p>@endif</section></main></html>
