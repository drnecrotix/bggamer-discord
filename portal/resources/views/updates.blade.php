<!doctype html><html lang="bg"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>CMS updates · BG-GAMER</title><link rel="stylesheet" href="{{ url('/portal.css') }}"><main class="editor"><a href="{{ url('/admin') }}">← Dashboard</a><p class="eyebrow">SYSTEM / GITHUB RELEASES</p><h1>CMS updates</h1><p class="muted">Текуща версия: {{ $current }}. Само Owner може да приложи проверен code-only release пакет. Архив с миграции или нови зависимости се инсталира ръчно след backup.</p>@if(session('status'))<p class="notice success">{{ session('status') }}</p>@endif@if($errors->any())<p class="error">{{ $errors->first() }}</p>@endif<form method="post" action="{{ url('/owner/updates/check') }}" class="update-check">@csrf<button class="text-button" type="submit">Провери GitHub сега ↻</button></form><section class="panel"><h2>{{ $release['version'] ?? 'Няма публикуван release' }}</h2>@if($release && ($release['ready'] ?? false))<p class="muted">GitHub SHA-256: {{ $release['digest'] }}</p>@if($available)<p class="notice success">Налична е нова версия.</p><form method="post" action="{{ url('/owner/updates') }}">@csrf<input type="hidden" name="version" value="{{ $release['version'] }}"><button class="button">Обнови до {{ $release['version'] }}</button></form>@else<p class="muted">Тази версия вече е инсталирана.</p>@endif @else<p class="muted">Очаква се GitHub Release с asset <code>bggamer-portal.zip</code> и SHA-256 digest.</p>@endif</section></main></html>
